/**
 * Shared component variant types (component inventory doc).
 * Full prop interfaces live with their components; only variants that
 * cross page/component boundaries belong here.
 */

export type HeroVariant = "home" | "page";
export type ServicesGridVariant = "grid" | "list";
export type ProcessVariant = "compact" | "detailed";
export type PortfolioVariant = "highlight" | "index";
export type CTABandVariant = "dark" | "accent";
